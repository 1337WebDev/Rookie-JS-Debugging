// This code is supposed to greet a person by their name, but it's throwing an error.
// The challenge is to identify and fix the bug.

function greet(name) {
  console.log("Hello, " name "!");
}

greet("Alice");
