// This code is supposed to print each number in the array, but it's throwing an error.
// The challenge is to identify and fix the bug.

let numbers = [1, 2, 3, 4, 5];

for (let i = 0; i <= numbers.length; i++) {
  console.log(numbers[i]);
}
